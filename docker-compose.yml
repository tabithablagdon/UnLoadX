version: '2'
services:
  web:
    image: apiserver
    ports:
     - "3000:3000"
     - "4000:4000"
    # mount project directory to code directory on container
    volumes:
     - .:/code
    command: npm start
